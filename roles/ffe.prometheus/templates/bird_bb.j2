---
{% for host in groups['routers'] %}

- targets: [ {{ host }}:9324 ]
  labels:
    job: bird_bb
    site: {{ host | regex_replace('[^.]+\.(.*?)(\.de)?\.ff-e\.net$', '\\1') }}
    scope: backbone
{% endfor %}
